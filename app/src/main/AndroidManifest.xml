<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sweprojekt">
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SWEProjekt">
        <activity
            android:name=".ui.list.showbuilding.ShowBuildingActivity"
            android:exported="false" />
        <activity
            android:name=".ui.home.newroom.newbuilding.NewBuildingActivity"
            android:exported="false" />
        <activity
            android:name=".ui.list.showroom.ShowRoomActivity"
            android:exported="false" />
        <activity
            android:name=".ui.home.newroom.NewRoomActivity"
            android:exported="false" />
        <activity
            android:name=".ui.home.qrcodescanner.CameraActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>